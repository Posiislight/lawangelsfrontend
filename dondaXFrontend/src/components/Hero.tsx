import React from "react";

const Hero: React.FC = () => (
  <section className="absolute inset-0 flex items-center justify-center bg-gray-100 dark:bg-gray-900 transition-colors duration-300 z-0">
    <div className="flex flex-col items-center justify-center">
      Hero Section
    </div>
  </section>
);

export default Hero;

